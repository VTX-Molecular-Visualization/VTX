#ifndef __VTX_CORE_NEW_STRUCT_DEFINE__
#define __VTX_CORE_NEW_STRUCT_DEFINE__

#include <concepts>

namespace VTX::Core::NewStruct
{
	// template<typename M>
	// concept ConceptMolecule = std::derived_from<M, Struct::Molecule>;

	// template<typename C>
	// concept ConceptChain = std::derived_from<C, Struct::Chain>;

	// template<typename R>
	// concept ConceptResidue = std::derived_from<R, Struct::Residue>;

	// template<typename A>
	// concept ConceptAtom = std::derived_from<A, Struct::Atom>;

	// template<typename B>
	// concept ConceptBond = std::derived_from<B, Struct::Bond>;
} // namespace VTX::Core::NewStruct
#endif
