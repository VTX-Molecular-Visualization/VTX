#include "app/core/ecs/base_entity.hpp"

namespace VTX::App::Core::ECS
{
	// BaseEntity::BaseEntity() { _entity = Manager::ECSManager::get().registry().create(); }
	// BaseEntity ::~BaseEntity() { Manager::ECSManager::get().registry().destroy( _entity ); }

} // namespace VTX::App::Core::ECS
