#ifndef __EN__
#define __EN__

#include <map>

namespace VTX
{
	namespace Localization
	{
		std::map<std::string, const char *> en
			= { { "MainMenu.Menu", "Menu" }, { "MainMenu.Menu.Quit", "Quit" } };

	} // namespace Localization
} // namespace VTX
#endif
